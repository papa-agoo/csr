////////////////////////////////////////////////////////////////////////////////////////////////////

#include <csr/core/base/guid.h>

////////////////////////////////////////////////////////////////////////////////////////////////////

static u64 g_guid = 0;

guid guid_create(u32 value)
{
    u64 magic = ++g_guid & 0xFFFFFFFF;

    return make_guid((magic << 32) | value);
}
