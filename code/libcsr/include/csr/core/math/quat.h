////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

#include "utils.h"

////////////////////////////////////////////////////////////////////////////////////////////////////

struct quat
{
    union
    {
        f32 data[4];

        struct {
            f32 x, y, z, w;
        };
    };
};

#define make_quat(...) \
    make_compound_literal(struct quat, __VA_ARGS__)

////////////////////////////////////////////////////////////////////////////////

CSR_INLINE struct quat quat_identity()
{
    return make_quat(0, 0, 0, 1);
}
