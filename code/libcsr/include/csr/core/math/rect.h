////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

#include <csr/core/base.h>

////////////////////////////////////////////////////////////////////////////////////////////////////

struct rect
{
    s32 x, y, w, h;
};

#define make_rect(...) \
    make_compound_literal(struct rect, __VA_ARGS__)

////////////////////////////////////////////////////////////////////////////////

CSR_INLINE f32 rect_get_aspect_ratio(struct rect rect)
{
    check_expr(rect.w > 0);
    check_expr(rect.h > 0);

    return (f32)rect.w / (f32)rect.h;

error:
    return 0;
}
