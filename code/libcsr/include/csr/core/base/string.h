////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

////////////////////////////////////////////////////////////////////////////////////////////////////

// make_string("popo")                              // popo
// make_string("/foo/%s/baz", "bar")                // /foo/bar/baz

// str = make_string("hello_popo.so")               // hello_popo.so
// string_replace(str, ".so", ".ini")               // hello_popo.ini
