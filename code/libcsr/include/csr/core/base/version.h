////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

#include "types.h"
#include "string.h"

////////////////////////////////////////////////////////////////////////////////////////////////////

struct version
{
    u32 major;
    u32 minor;
    u32 micro;
};

#define make_version(...) \
    make_compound_literal(struct version, __VA_ARGS__)

struct string version_str(struct version v);
