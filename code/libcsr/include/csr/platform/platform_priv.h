////////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

#include "platform.h"

////////////////////////////////////////////////////////////////////////////////////////////////////

struct platform
{
    bool is_initialized;

    struct platform_info info;
    struct platform_callbacks callbacks;
};

struct platform* platform_ptr();
